//#include <stdio.h>
//
//int main()
//{
	/*int num = 10;

	if (num > 10 || num < 10) {
		printf("거짓입니다.");
	}
	else {
		printf("참입니다.");
	}*/
	/*int num;
	int num2;
	printf("숫자를 입력하세요: ");
	scanf_s("%d", &num);
		while (1) {
			printf("추측할 숫자를 입력하세요: ");
			scanf_s("%d", &num2);

			if (num2 > num) {
				printf("추측한 숫자 보다 작다.\n");
			}
			else if(num2 < num) {
				printf("추측한 숫자 보다 크다.\n");
			}
			else {
				printf("정답입니다.\n");
				break;
			}
		}*/
//	return 0;	
//}