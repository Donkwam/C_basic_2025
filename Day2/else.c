/* 1 ~ 10까지 숫자중 홀수만 출력 */
//#include <stdio.h>
//
//int main()
//{
	//int i = 0;
	//for (int i = 1; i <= 10; i++) {
	//	if (i % 2 == 0) continue;		// continue는 건너뛴다. 즉 나머지가 0이면 출력 X
	//	//if (i == 7) break;				// break는 반복문을 빠져나온다. 즉 7이 되면 멈춘다.
	//	if (i >= 7) break;
	//	printf("%d\n", i);
	//}

	/*int i = 0;
	int j = 0;
	for (int i = 2; i <= 9; i++) {
		for (int j = 1; j <= 9; j++) {
			printf("%d * %d = %d\t", j, i, i * j);
			if (j == 9) {
				printf("\n");
			}
		}
	}*/

	/*int i = 0;
	int j = 0;
	printf("몇단을 출력할까요?");
	scanf_s("%d", &i);
	for (int j = 1; j <= 9; j++) {
		printf("%d * %d = %d\n", i, j, i * j);
	}*/
	
//	return 0;
//}